# chat_ui

Enterprise AI Chat UI (ChatGPT / Gemini / Perplexity style)

Phase 1:
- UI shell only
- No LLM calls
- No backend dependency

Local dev (optional backend)
- Create `chat_ui/.env.local` with `CORE_API_BASE=http://127.0.0.1:8000`
- Restart `npm run dev` after changing env vars
