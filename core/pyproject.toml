[project]
name = "klynx-core"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "fastapi>=0.110",
  "uvicorn>=0.23",
  "httpx>=0.27",
  "python-dotenv>=1.0",
  "pydantic>=2.6",
  "openai>=1.0",
  "requests>=2.31",
]

[tool.uvicorn]
factory = false
